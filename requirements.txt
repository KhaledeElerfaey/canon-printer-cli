# Canon Printer CLI Dependencies

# Core dependencies
requests>=2.28.0
PyYAML>=6.0

# Cross-platform printing and system utilities
Pillow>=9.0.0          # For image processing and conversion

# Windows-specific dependencies
pywin32>=227; sys_platform == "win32"           # Windows API access
wmi>=1.5.1; sys_platform == "win32"            # Windows Management Instrumentation

# Unix/Linux dependencies (not macOS - uses built-in CUPS)
python-cups>=1.9.73; sys_platform == "linux"   # For direct CUPS integration on Linux

# Optional network discovery
zeroconf>=0.39.0       # For Bonjour/mDNS discovery (cross-platform)

# Development dependencies (optional)
# pytest>=7.0.0         # For testing
# black>=22.0.0          # For code formatting
# flake8>=4.0.0          # For linting

# Platform-specific system requirements:
#
# Windows:
# - No additional system packages needed (uses Windows Print Spooler)
# - Optional: Install Canon drivers and utilities
#
# macOS/Linux:
# - cups (print system)
# - cups-client (print utilities like lpr, lpstat)  
# - libreoffice (for Office document conversion)
#
# All platforms:
# - Canon printer drivers installed
# - Network connectivity for IPP discovery
